import { BoldOnClickDirective } from './bold-on-click.directive';

describe('BoldOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
