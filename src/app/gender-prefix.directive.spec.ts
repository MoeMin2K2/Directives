import { GenderPrefixDirective } from './gender-prefix.directive';

describe('GenderPrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderPrefixDirective();
    expect(directive).toBeTruthy();
  });
});
